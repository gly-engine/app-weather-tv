{
  "scripts": {
    "postinstall": "npm run build && echo 'declare const node: {}\nexport default node' > dist/game.d.ts",
    "build": "gly-cli build-html src/main.lua"
  },
  "devDependencies": {
    "@gamely/gly-cli": "^0.2.1"
  }
}
