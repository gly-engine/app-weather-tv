local Top = {}

function Top:load(std)
    -- local grid = std.ui.grid('14x1')
    --     :add({},11)
    --     :add(std.node.load('src/ui/section/MenuInfos.lua'), 3)
    -- local info = grid:get_item(2)
    -- info.data.background = true
end

return Top
